[Definition]
# Only ban high-confidence malicious WAF denies (avoid 429/rate-limit false positives).
failregex = ^\[[^\]]+\]\s+action=deny\s+reason=(?:signature|headless-stealth)\s+ip=<HOST>\s+path=.*$
ignoreregex =
