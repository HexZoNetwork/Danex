# managed by pteroprotect setup.sh
# real_ip trust chain for CDN / reverse proxy edges
# Keep this disabled unless every trusted proxy/CDN CIDR is explicit.
# Applications and sidecars should consume nginx $remote_addr via X-Real-IP
# only after nginx has applied these trusted set_real_ip_from rules.
# real_ip disabled (set network.real_ip_enabled=true and provide trusted_proxy_*_cidrs)
# no set_real_ip_from entries were applied
